-record(user, {user_id,
	       remote_user_id,
	       remote_auth_type,
	       name,
	       role,

	       balance,
	       currency_alpha,
	       currency_number,
	       country_number,

	       ban,
	       ban_comment,
	       params,

	       last_ip_alpha,

	       status,
	       partner_id,
	       session_id
	      }).
